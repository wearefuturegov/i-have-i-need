applications:
  - name: beacon-support
    memory: 1024M
    buildpacks:
      - https://github.com/cloudfoundry/ruby-buildpack.git#v1.8.40
    services:
      - beacon-postgres
      - beacon-postgres-audit-logs
    health-check-type: http
    health-check-http-endpoint: /ready
